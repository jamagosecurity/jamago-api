bin/
obj/
.vs/
*.user
appsettings.Development.json
appsettings.Production.json
deploy/appsettings.Production.json
PGADMIN-VPS-CONNECT.txt
GOLDEN-RULES.txt
scripts/node_modules/
scripts/package.json
scripts/package-lock.json
scripts/*.mjs
publish/
.tools/
logs/

# Uploaded content (camera photos, VIP client documents, drawing files) —
# runtime data written by LocalFileStorage, never source. A handful of these
# were committed by accident before this rule existed and have since been
# untracked (kept on disk, just no longer version-controlled).
Jama.Web/storage/

PRODUCTION-CREDENTIALS.local.txt
deploy/PRODUCTION-CREDENTIALS.local.txt
